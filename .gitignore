# Node dependencies — regenerated by `npm install` from package-lock.json
node_modules/

# macOS Finder metadata
.DS_Store

# Editor + log noise
*.log
npm-debug.log*
.vscode/
.idea/
# Build output — regenerated by electron-builder, must not be tracked
# (contains macOS .app symlinks that break on Windows checkouts)
release/
